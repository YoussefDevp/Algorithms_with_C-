#include <iostream>
#include <string>  

using namespace std;

enum mySkills
{
    HTML, CSS, javaScript
};

enum status
{
    Single, Married
};

enum gander
{
    Male, Femmal
};

int main()
{
    cout << to_string(mySkills::CSS);
    textBox1->AppendText("Hello World");
}
